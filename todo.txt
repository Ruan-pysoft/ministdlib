More complete memory.h
	memcpy, memmove, etc
	make memzero more efficient

Add dedicated close function to FILE
	should always be called to free resources when done with file

Move over from sbrk to mmap
	Maybe also use sbrk for small values?

implement multithreading

rethink strings.h

implement buffered io

Upgrade the test suite

...

printf-style suite

scanf-style suite

high-level input (have puts, putc, etc, but no getc, gets, getline, etc)

ANSI escape sequence helpers

proper error handling (enum?)
	+ errstr

move argv & envp to gobal (read-only) structure
